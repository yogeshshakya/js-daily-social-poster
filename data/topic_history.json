[
  {
    "date": "2026-09-12",
    "topic": "Why mutating a ref's .current property during the render phase violates React's concurrent rendering model and causes unpredictable, hard-to-debug UI tearing"
  },
  {
    "date": "2026-09-13",
    "topic": "Why using the `delete` keyword to remove a property from an object transitions V8 to dictionary mode, permanently destroying the hidden class (Shape) optimization and causing massive performance drops in hot paths"
  },
  {
    "date": "2026-09-13",
    "topic": "Why wrapping a state update in `startTransition` still blocks the UI during expensive renders unless the slow components are memoized, because React still has to execute the entire tree's render phase."
  },
  {
    "date": "2026-09-13",
    "topic": "Why an unused large variable leaks memory in JavaScript when an adjacent closure in the same lexical scope is retained, due to how V8 shares closure contexts across functions"
  },
  {
    "date": "2026-09-13",
    "topic": "Why pre-allocating arrays with 'new Array(size)' or manually resizing them via 'length' transitions V8 to 'holey' element kinds, permanently degrading iteration performance compared to packed arrays"
  },
  {
    "date": "2026-09-14",
    "topic": "Why using React 19's new `use` hook to resolve a Promise inside a component causes an infinite re-render loop if the Promise is instantiated directly during render instead of being cached with `React.cache` or `useMemo`"
  },
  {
    "date": "2026-09-14",
    "topic": "Why throwing an error inside a `new Promise` executor function fails to reject the promise if the throw occurs inside an asynchronous callback (like `setTimeout`), leading to uncaught runtime exceptions instead of a rejected promise."
  }
]